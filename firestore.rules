rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Semua akses hanya via backend (Cloud Functions)
    // Frontend tidak boleh akses Firestore langsung
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
