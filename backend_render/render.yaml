services:
  - type: web
    name: spab-krl-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 2 -b 0.0.0.0:$PORT app:app"
    envVars:
      - key: FIREBASE_CREDENTIALS_JSON
        sync: false
